import { API_URL } from '../env';

const AppConfig = {
  /**
   * Url base of API
   * @type string
   */
  apiUrl: API_URL,

  /**
   * Is the app in debug mode or not
   * @type boolean
   */
  debug: true,
};

export default AppConfig;